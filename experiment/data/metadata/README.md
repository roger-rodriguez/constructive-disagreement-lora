# Metadata

Frozen sidecar metadata for every reported dataset split lives here.
Labels, taxonomy fields, and review provenance are never included in model
input. The sidecars contain final generation, reviewer, adjudication, and
human-audit states; raw review transcripts are intentionally excluded.
